{
    "configurations": [
        
        {
            "name": "composeApp [Desktop] (1)",
            "type": "gradle",
            "workingDir": "$PROJECT_DIR$",
            "tasks": ["desktopRun"],
            "args": ["-DmainClass=dev.donmanuel.kmpmovies.MainKt", "--quiet", "-p", "$PROJECT_DIR$/composeApp"],
            "initScripts": {
                "flmapper": "ext.mapPath = { path -> path }"
            }
        }
    ]
}